# 捏造脱出計画

作成日: 2026-08-29
対象: Codex Delivery Assurance repository と Skill Magnet 配布設定

## 1. 目的

証拠がない状態で「完成」「リリース可能」と判定した進め方を止め、観測事実、変更、検証、配布物、公開状態を一対一で追跡できる状態へ戻す。

本計画では、次を禁止する。

- 未実行の試験を合格と書く
- 存在しないZIP、checksum、tag、公開commitを存在すると書く
- 作業tree、commit、remote、Skill Magnet snapshotを同じものとして扱う
- 自分で作成した文書だけを根拠にGO判定する
- 履歴の書換えやforce pushで失敗の痕跡を消す

## 2. 開始時点の観測事実

| 対象 | 2026-08-29の観測値 | 判定 |
|---|---|---|
| local branch | `main` | 観測済み |
| local HEAD | `787770cccd349d162ceac0596acd9a85dfa0b1c6` | 観測済み |
| `origin/main` | `225acbe63d0eecdc3617f443dcb495168a482ef4` | 観測済み |
| 差分 | localが1 commit先行 | 未解消 |
| 先行commit | `bd init: initialize beads issue tracking` | 製品範囲外 |
| 先行commitの内容 | `.beads` 4 fileと`.gitignore`追記 | 観測済み |
| 作業tree | staged削除4件、unstaged変更6件 | dirty |
| Git tag | なし | 未作成 |
| 配布ZIP | なし | 未作成 |
| checksum | なし | 未作成 |
| Skill Magnet固定commit | `c7747bba0bc391316aa558b3b4e8dd412045d2dc` | stale |
| 文書version | `0.1.0` / `Unreleased` が混在 | 未確定 |

## 3. 原因

### 3.1 対象範囲の混同

Delivery Assuranceの成果物と、調査対象として一時的に参照した課題管理製品を混同した。その結果、製品要件にない初期化処理をrepositoryへ入れた。

### 3.2 状態変更後の閉じ忘れ

`787770c` は `.beads` の生成物を追加しただけのcommitである。その後、生成物4件の削除だけをstageし、`.gitignore`、文書、配布、検証、commitまでを一つの是正作業として完了しなかった。

安全な是正方法は、`787770c` を履歴から消すことではない。通常の是正commitで生成物とignore追記を除去し、何が起きたかを履歴に残す。

### 3.3 証拠と判定の順序逆転

commit、tag、ZIP、checksum、固定snapshot、固定候補に対する再試験が揃う前に、リリース可否を文章で先に判定した。文書の記載を実物の代替にしたことが直接原因である。

### 3.4 配布面の同期漏れ

表示名をDelivery Assuranceへ変更した一方で、Skill Magnetの取得元commitと承認snapshotを更新しなかった。UI上の名称と実際に実行されるskill原本が一致していなかった。

## 4. 実行手順

| 順序 | 作業 | 完了証拠 |
|---|---|---|
| 1 | 製品範囲外の生成物とignore追記を通常の差分で除去する | `git diff` と是正commit |
| 2 | 9 skillをDelivery Assurance本体、orchestrationを任意別Skillとして文書を統一する | README、INDEX、manifestの整合 |
| 3 | versionを`0.2.0`、release dateを`2026-08-29`へ固定する | release notesと配布文書 |
| 4 | 9 skillだけを含む配布ZIPとSHA-256を生成する | `dist`の実物と再計算結果 |
| 5 | JSON、frontmatter名、相対link、禁止語、配布内容を自動検証する | 検証commandの終了code 0 |
| 6 | 固定release candidateをcommitする | commit hash |
| 7 | Skill Magnetのpackage、承認snapshot、expected commitをrelease candidateへ更新する | 設定値、snapshot gitlink、test結果 |
| 8 | context menuを再生成し、表示名と実体を照合する | installed manifest |
| 9 | public repositoryとSkill Magnet repositoryをそれぞれcommit・pushする | local/remote hash一致 |
| 10 | public repositoryへ`v0.2.0` tagを付与してpushする | local/remote tag一致 |
| 11 | 実測値だけで「捏造脱出実績.md」を作成する | evidence tableと未解消0件 |

## 5. リリース判定条件

次の全条件が同時に満たされた場合だけGOとする。

- 作業treeがcleanである
- public `main` のlocal/remote hashが一致する
- `v0.2.0` のlocal/remote tagが同一commitを指す
- ZIPが存在し、9 skillだけを標準packageとして含む
- 公開checksumと再計算値が一致する
- 9 skillの構造、JSON、link、名称、禁止語の検証が全件合格する
- Skill Magnetのactive pack IDが`codex-delivery-assurance`である
- Skill Magnetのexpected commit、承認snapshot、package sourceが公開release commitと一致する
- Skill Magnetの全自動testが固定snapshotに対して合格する
- 未commit変更、未push commit、未解消blockerが0件である

1件でも欠ける場合はNO-GOとし、「予定」ではなく不足している実物を明記する。

## 6. rollback

- 履歴はrewriteしない。
- 是正後に不具合が見つかった場合は、対象commitを明示した通常のrevertまたは追加修正commitを使う。
- 公開済みtagは同名で移動せず、新しいpatch versionを発行する。
- Skill Magnetは直前の承認snapshot hashを記録し、更新失敗時だけその固定hashへ戻す。

## 7. 記録規則

- 「観測済み」「推定」「未実行」を分ける。
- commandの終了code、commit hash、file hash、件数を記録する。
- harness合格を製品合格の代わりにしない。
- 作成者自身の判定だけで完了にせず、機械的な再照合結果を必須にする。
