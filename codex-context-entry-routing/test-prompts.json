{
  "skill": "codex-context-entry-routing",
  "version": "0.1.0",
  "source_book": "Codex CLI official documentation — OpenAI",
  "darwin_compatible": true,
  "test_cases": [
    {"id":"should-trigger-01","type":"should_trigger","prompt":"UIのspacing差を直したい。screenshot、文章、MCPのどのcontextをCodexへ渡す？","expected_behavior":"visual evidenceにはimageを選び、inspect対象・制約・done criteriaを添える。","notes":"image入口。"},
    {"id":"should-trigger-02","type":"should_trigger","prompt":"社内API仕様と今日の公開料金表を照合したい。MCPとweb searchをどう使い分ける？","expected_behavior":"情報の所在と鮮度でMCPとlive searchを選び、下位security controlへ渡す。","notes":"複数入口。"},
    {"id":"should-trigger-03","type":"should_trigger","prompt":"前のCodex taskの判断履歴を残したまま別案を試したい。resumeかforkかも含めて決めて。","expected_behavior":"既存履歴をcontext入口として評価し、代替案ならforkを選ぶ。","notes":"conversation入口。"},
    {"id":"should-not-trigger-01","type":"should_not_trigger","prompt":"MCPのupdate toolを無効化し、read toolだけapprovalなしにしたい。","expected_behavior":"本skillではなくcodex-mcp-control-planeを使う。","notes":"跨skill混同: 入口選択後のcontrol。"},
    {"id":"should-not-trigger-02","type":"should_not_trigger","prompt":"local repository内のREADMEだけを要約して。","expected_behavior":"外部context入口が不要なので本skillを使わない。","notes":"local contextで完結。"},
    {"id":"edge-01","type":"edge_case","prompt":"screenshotだけ添付した。いい感じに直して。","expected_behavior":"image入口は適切だがtask language不足として、対象・期待結果・制約・完了条件を具体化してから使う。","notes":"入口は合うが契約不足。"}
  ],
  "minimum_pass_rate": 0.8,
  "notes": "3 positive + 2 bait + 1 edge。should_not_triggerは全件必須。"
}

