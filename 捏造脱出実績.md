# 捏造脱出実績

作成日: 2026-08-30

## 1. 結論

Codex Delivery Assurance `0.2.0` はリリース済みである。

この結論はREADMEの記載だけではなく、公開commit、tag、配布ZIP、checksum、Skill Magnet承認snapshot、Windows実機menu、Windows/macOS CIの実測値に基づく。

## 2. 何が誤っていたか

### 製品範囲外commit

`787770cccd349d162ceac0596acd9a85dfa0b1c6` は、製品機能ではなく課題管理toolの初期化生成物4件とignore追記だけを加えていた。これをrelease候補から曖昧に「外す」と表現したことも誤りだった。

実際に行った是正は履歴削除ではない。通常commit `8f12af5ddfdd3b985f26d33dad09d6061d675342` で生成物とignore追記を除去し、事故commitは監査可能な履歴として残した。

### incompleteな取消し

開始時点では生成物4件の削除だけがstageされ、`.gitignore`、名称、version、配布物、test、commit、pushが別々の未完了状態だった。削除をstageしただけで是正済みのように説明したことが問題だった。

### 証拠より先のGO判定

開始時点にZIP、checksum、tagは存在せず、Skill Magnetは旧commit `c7747bba…` を参照していた。にもかかわらず、文書上の予定を完成状態のように扱った。以後は、実物がない項目をGO条件へ数えていない。

## 3. 実施した是正

| 項目 | 実施結果 |
|---|---|
| 製品範囲 | Delivery Assurance本体9 skillへ固定 |
| 別Skill | `codex-pmo-orchestration` は任意・別配布へ分離 |
| 製品外生成物 | 通常commitで削除。履歴rewriteなし |
| version | `0.2.0` |
| 配布ZIP | `dist/codex-delivery-assurance-0.2.0.zip` |
| payload checksum | `CHECKSUMS.sha256` をZIP内外へ生成 |
| ZIP checksum | `a850dc495ecbddcf438b7ffcb7a9ca2ff31b810a076b7a7b9f195983e90207bf` |
| release commit | `8f12af5ddfdd3b985f26d33dad09d6061d675342` |
| release tag | `v0.2.0`。local/remoteともrelease commitへpeel |
| Skill Magnet pack ID | `codex-delivery-assurance` |
| Skill Magnet表示名 | `Delivery Assurance` |
| Skill Magnet snapshot | `codex-delivery-assurance-8f12af5` |
| Skill Magnet expected commit | `8f12af5ddfdd3b985f26d33dad09d6061d675342` |
| Skill Magnet main | merge commit `aad06ffc7eb33a11e2abcd42f3f191fe8dfb5309` |

## 4. 検証実績

### Delivery Assurance repository

- 9 skill directoryの存在: PASS
- `SKILL.md` frontmatter名とdirectory名: PASS
- `acceptance.json` / `test-prompts.json` parse: PASS
- relative Markdown link: PASS
- 配布面の製品外参照: 0件
- ZIPに9 skillを収録: PASS
- ZIPに別Skill `codex-pmo-orchestration` を収録しない: PASS
- ZIP checksum再計算一致: PASS
- `git diff --check`: PASS

### Skill Magnet

- local full suite: 125 tests実行、124 PASS、1環境依存skip、FAIL 0
- canonical wheel payload SHA-256: `de2be0d0415e398369ae1464f0b8759f401863ac3f07c56da0df95b4bbd52a86`
- Windows CI: PASS。125 test、wheel gate、certificate test、wheel内native build、MSIX lifecycle
- macOS CI: PASS。125 test、wheel gate、Finder Quick Action install、Automator実行、uninstall、残留ゼロ
- release branch GitHub Actions run: `33259524623`
- merged `main` GitHub Actions run: `33259715964`。Windows/macOSともPASS
- pull request: `#1`、merged

### Windows実機menu

更新wheelをsite-packagesへ再installしてcontext menuを再生成した。

- `usable_installed_state`: `true`
- `menu_contract_valid`: `true`
- `menu_contract_matches_config`: `true`
- leaf count: `1`
- selection kind: `package`
- 表示名: `Delivery Assurance`
- pack ID: `codex-delivery-assurance`
- 固定commit: `8f12af5ddfdd3b985f26d33dad09d6061d675342`

最初の再生成では、実機menuが旧site-packagesを使っており、`PMO / c7747bba` のままだった。これをPASSにせず、wheelを実installして再生成した後に上記値を確認した。

## 5. 検証中に検出して修正した失敗

| 失敗 | 原因 | 修正 |
|---|---|---|
| 禁止語検査が計画書を検出 | 製品配布面と事故記録を同じ範囲で検索 | 検査対象を9 skillと利用文書へ限定 |
| buildが成功表示後に非0終了 | validator内の検索終了codeが親scriptへ残留 | 成功時の終了codeを明示的に0へ正規化 |
| 実機menuが旧名称・旧commit | source treeではなく既存site-packages版を実行 | 更新wheelを再installしmenuを再生成 |
| 初回公開CIがwheel gate失敗 | 過去のledger SHAを新snapshotへ転用 | Windows/macOS CI artifactを取得してcanonical hashを固定 |
| ローカルwheel hashとCI wheel hashが不一致 | ローカルbuild物を公開artifactの代替にした | CI保存wheelを正本とし両OS一致を確認 |
| 計画書に末尾空白1件 | 初回commit前の連結commandが検査後もcommitを続行 | 最終監査commitで除去し再検査 |

## 6. 再発防止

- release判定は、commit、remote、tag、artifact、checksum、test、実機の各面を別々に観測する。
- CI artifactのhashをローカルartifactから推定しない。
- source更新後は、実install先とmenu manifestを再確認する。
- 文書のGO表現は、固定candidateのCI完了後にだけ更新する。
- 失敗したrunと誤ったhashは削除せず、修正理由と正しいrunを記録する。
- 製品外toolの初期化を製品repositoryへcommitしない。

## 7. リリース判定

`v0.2.0` の判定は **GO**。

根拠は次の同一性である。

1. public release tag `v0.2.0` → `8f12af5…`
2. 配布ZIPとchecksum → `8f12af5…` に収録
3. Skill Magnet expected commit / snapshot / installed menu → `8f12af5…`
4. Skill Magnet artifact-input commit `df8e15f…` → cross-platform run `33259524623`でPASS
5. Skill Magnet変更 → PR #1で`main`へmerge済み

Microsoft Store等の外部Store公開はこのGOに含めない。今回のGOは、repository、ZIP配布、Skill Magnet同梱、Windowsローカルmenu、macOS Finder CIで定義したrelease範囲に対する判定である。
